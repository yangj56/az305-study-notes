Azure AD Connect with seamless SSO => Your organization uses Azure AD and on-premises AD. Users need SSO to both cloud and on-premises resources.

Monitor Workbooks => Which Azure service provides custom reports on Azure AD sign-ins and audit events?

AD Conditional Access => You need to ensure that only specific IP ranges can access the Azure management plane (Azure Portal, ARM API). What should you configure?

AD Password Protection => Which Azure AD feature prevents users from reusing previous passwords?

OAuth 2.0 client credentials flow = > You need to grant an application permissions to read Azure AD user profiles. Which authentication method should you use?

AD Application Proxy = Which component is required to extend Azure AD conditional access policies to on-premises applications?

SigninLogs = To monitor Azure AD sign-in failures, which Log Analytics table

A deny policy blocks storage account creation if public blob access is enabled. Modify would alter settings but not prevent deployment.

Dynamic groups auto-include users based on rules (e.g., department=finance). Access Packages manage access requests.

Azure Policy’s regulatory compliance dashboard maps built-in policies to standards (e.g., ISO 27001) and shows compliance status. = Which Azure service provides centralized management of multi-subscription compliance with industry standards (e.g., ISO 27001)?

Conditional Access can require MFA when accessing M365 apps from non-Hybrid Azure AD joined or compliant devices.

CIS Benchmark policies include rules like disabling password-based SSH authentication.

Conditional Access can block guest access to non-approved apps. B2B collaboration governs invitation workflows, not app restrictions.

Contributor allows full resource management but not RBAC assignments. Owner permits RBAC changes.

SigninLogs includes Conditional Access status (success/failure) per sign-in attempt.

Blueprints define a repeatable set of governance artifacts (policies, RBAC, ARM) for new subscriptions.

PIM eligibility requires users to activate time-bound privileged roles only when needed, reducing standing privileges and enabling approval requirements.

A deny effect prevents non-compliant resource creation, enforcing TLS 1.2 at deployment.

Conditional Access can block sign-ins from named locations/countries not in the allowed list.

SCIM auto-deprovisions SaaS app access when Azure AD accounts are disabled via HR integration.
SCIM (System for Cross-domain Identity Management) is a protocol that enables automated provisioning and deprovisioning of user accounts in SaaS applications, based on data from an HR system or other authoritative source.

The built-in Reader role includes permissions to view policy compliance data. Which RBAC permission allows viewing Azure Policy compliance status but not modifying policies?

Diagnostic settings in Azure AD can stream sign-in logs to Event Hubs for real-time analysis.

Azure AD MFA doesn’t support X.509 certificates; it uses FIDO2, SMS, OATH tokens, etc.

Azure Lighthouse enables cross-tenant management, including Conditional Access policy administration.

`isnull(tags)` identifies resources with no tags in Kusto Query Language (KQL).

Privileged Role Administrator configures PIM without having standing Global Admin rights.

Azure AD Login extension enables SSO to VMs using Azure AD credentials.

Reporting APIs allow programmatic export of user data for compliance requests. such as For GDPR compliance

External collaboration settings in Azure AD restrict guest invites by domain.

Advisor recommends removing unused role assignments after 90 days of inactivity.

PIM for Azure SQL enables time-bound DBA role activation without persistent accounts.

PRT on hybrid Azure AD joined devices provides continuous authentication, reducing MFA prompts.

AzureActivity logs track subscription-level operations like policy assignment modifications.

Intune manages device compliance status, which Conditional Access policies reference.

Policy’s modify effect auto-applies tags during resource group creation.

Reports Reader grants access to sign-in and audit logs without security policy visibility.